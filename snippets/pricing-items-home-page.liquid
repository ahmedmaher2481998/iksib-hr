<script>
  var myProductVariable = {{ thecollection json }};
  console.log('Data', myProductVariable);
</script>


<section class="bg-white ">
  <h1 class="text-center text-bold ">
    Title : {{ thecollection.products_count }}
  </h1>


  <div class="py-8 px-4 mx-auto max-w-screen-xl lg:py-16 lg:px-6">
    <div class="mx-auto max-w-screen-md text-center mb-8 lg:mb-12">
      <h2 class="mb-4 text-4xl tracking-tight font-extrabold text-gray-900 " id="title"></h2>
      <p class="mb-5 font-light text-gray-500 sm:text-xl ">
        Pay smarter with HR. Our intelligent payroll system is designed to help businesses of all sizes do their best work. With HR, you can manage your entire team’s pay and benefit packages, including taxes, deductions and garnishments. And because we’re always working to improve our software, your business will stay protected as you grow bigger.</p>
    </div>
    <div class="space-y-8 lg:grid lg    :grid-cols-3 sm:gap-6 xl:gap-10 lg:space-y-0">
      {% for product in thecollection.products %}
        {{ product.title }}
          {% if forloop.index == 1 %}
          <!-- Pricing Card -->
          {% for variant in product.variants %}
            {{ variant.title }}
              <form
              action="{{routes.cart_add_url}}"
              data-productid="{{variant.id}}"
              method="post"
              class="flex flex-col p-6 mx-auto max-w-lg text-center text-gray-900 bg-white rounded-lg border border-gray-100 shadow ">
              <input
                type="hidden"
                name="id"
                data-productid="{{product.id}}"
                class="product-select"
                value="{{ variant.id }}"
                data-variant-title="{{ variant.title }}" />
              <h3 class="mb-4 text-2xl font-semibold">Starter</h3>
              <p class="font-light text-gray-500 sm:text-lg ">{{ variant.title }}</p>
              <div class="flex justify-center items-baseline my-8">
                <span class="mr-2 text-5xl font-extrabold">{{ variant.price | money_without_trailing_zeros }}</span>
                <!-- 
                <span class="text-gray-500 ">/month</span>
                -->
              </div>
              <!-- List -->
              <ul role="list" class="mb-8 space-y-4 text-left">
                <li class="flex items-center space-x-3">
                  <!-- Icon -->
                  {% render 'marck-check' %}
                  <span>Individual configuration</span>
                </li>
                <li class="flex items-center space-x-3">
                  <!-- Icon -->
                  {% render 'marck-check' %}
                  <span>No setup, or hidden fees</span>
                </li>
                <li class="flex items-center space-x-3">
                  <!-- Icon -->
                  {% render 'marck-check' %}
                  <span>Team size:
                    <span class="font-semibold">1 developer</span>
                  </span>
                </li>
                <li class="flex items-center space-x-3">
                  <!-- Icon -->
                  {% render 'marck-check' %}
                  <span>Premium support:
                    <span class="font-semibold">6 months</span>
                  </span>
                </li>
                <li class="flex items-center space-x-3">
                  <!-- Icon -->
                  {% render 'marck-check' %}

                  <span>Free updates:
                    <span class="font-semibold">6 months</span>
                  </span>
                </li>
              </ul>
              <button class="text-black bg-primary hover:bg-secondary-darker  focus:ring-4 focus:ring-secondary-darker font-medium rounded-lg text-sm px-5 py-2.5 text-center">Get started</button>
            </form>
          {% endfor %}

        {% endif %}
      {% endfor %}

    </div>
  </div>
</section>